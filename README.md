# HF Watch

A command-line tool that shows current HF propagation conditions using solar indices.

## Install

Requires Python and pipx.

```bash
pipx install git+https://github.com/Gdev45/HF-conditions-CLI.git
