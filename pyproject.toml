[project]
name = "hf-watch"
version = "1.0.0"
description = "HF propagation conditions CLI"
requires-python = ">=3.9"

dependencies = [
    "requests",
    "rich"
]

[project.scripts]
hf-watch = "hf_watch.cli:main"
