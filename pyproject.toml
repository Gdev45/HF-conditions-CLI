[project]
name = "hf-watch"
version = "1.0.0"
description = "HF propagation monitor CLI using solar indices"
authors = [
    {name="Gdev45"}
]
requires-python = ">=3.9"
dependencies = [
    "requests"
]

[project.scripts]
hf-watch = "hf_watch.cli:main"
